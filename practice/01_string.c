#include <stdio.h> 

int main(){
    // char a[] = {'a','b','c'};
    // char a[] = "abc"; // both are same
    // for (int i = 0; i < 3; i++){
    //     printf("The chracter is %c\n",a[i]);
    // }
    // printf("%s",a);

    // char a[30];
    // scanf("%s",a);
    // gets(a);
    // printf("%s",a);
    // puts(a);
    // printf("Faisal");

    
     return 0;
}